# 核心架构

## 技术栈

```
前端: Vue.js 3 + Element Plus + TypeScript + Vite
后端: pring Boot 3.3 + Java 21 + MyBatis Plus
数据库: MySQL (生产) + H2 (开发/桌面版)
图表库: AntV (G2Plot, L7, S2) + ECharts
数据处理: Apache Calcite + Apache SeaTunnel
基础设施: Docker + APISIX
```

## 项目模块结构

```
dataease/
├── core/                    # 核心模块
│   ├── core-backend/        # 后端服务 (Spring Boot)
│   └── core-frontend/       # 前端应用 (Vue.js)
├── sdk/                     # SDK模块
│   ├── api/                 # API接口定义
│   ├── common/              # 公共组件
│   ├── distributed/         # 分布式组件
│   └── extensions/          # 扩展组件
├── installer/               # 安装部署相关
├── drivers/                 # 数据库驱动
├── mapFiles/                # 地图数据文件
├── docs/                    # 文档
└── de-xpack/                # 企业版扩展
```

# 运行项目

1. 创建数据库

   ```
   CREATE DATABASE dataease10 CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
   ```

2. 运行后端

   1. 用IDEA打开项目
   2. 找到core/core-backend/pom.xml, 右击/添加为Maven项目
   3. 右侧Maven面板, 点击刷新按钮(同步所有Maven项目)
   4. 左下 添加服务/运行配置/SpringBoot; 有效配置文件 填写'standalone'; 修改选项/勾选 缩短命令行, JAR清单

3. 运行前端

   1. 进入前端目录

     ```
     cd core/core-frontend
     ```
   2. 安装依赖

     ```
     npm install
     ```
   3. 启动开发服务器

     ```
     npm run dev
     ```
   4. 访问应用
   前端启动后，访问 http://localhost:8080 即可使用DataEase应用。

# 部署方式

1. 一键安装脚本
2. Docker容器化部署
3. 1Panel应用商店
4. 离线安装包